import { UserQuest } from './type'

export const defaults: UserQuest = {
  name: 'fp-ts-lib',
  homepage: 'http://',
  projectVersion: '1.0.0',
  license: 'MIT',
  prettier: true,
  eslint: true,
  jest: true,
  fastCheck: true,
  docsTs: true,
  ghActions: true,
  vscode: true,
  markdownMagic: true,
}
