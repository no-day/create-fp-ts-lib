{
  "name": "{{NAME}}",
  "homepage": "{{HOMEPAGE}}",
  "version": "{{VERSION}}",
  "main": "dist/index.js",
  "license": "{{LICENSE}}",
  "peerDependencies": {},
  "dependencies": {},
  "devDependencies": {
    "typescript": "^4.2.3"
  },
  "scripts": {
    "build": "yarn tsc",
    "build:watch": "yarn tsc --watch",
    "prepublish": "yarn build"
  }
}
